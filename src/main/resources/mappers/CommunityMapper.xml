<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--
    namespace : 해당 XML파일을 어떤 Mapper 인터페이스와 연결할지를 정해주는 속성.
-->
<mapper namespace="com.semi.easycoding.community.mapper.CommunityMapper">

    <!--
        <sql id="..."> : 여러 <select> 태그에서 공통으로 쓰는 sql 조각을
        한 곳에 정의해두고, 원하는 곳에 <include refid="...">로 재사용 가능
    -->
    <sql id="searchWhere">
    <!--
        <where> : 내부의 <if>들 중에 하나라도 참이면 자동으로 WHERE키워드를 붙여주고,
        맨 앞에 남는 불필요한 AND/OR은 자동으로 제거해준다.
    -->
        <where>
            <if test="postCategory != null
                    and postCategory != ''
                    and postCategory != 'all'">
                AND c.category = #{postCategory}
            </if>

            <if test="keyword != null and keyword != ''">
                AND p.title LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </sql>


    <!-- 게시글 목록 조회 sql문 -->
    <select id="selectPostList" parameterType="PostSearchCondition" resultType="PostDto">
        SELECT p.post_id, c.category, p.title, p.content, m.nickname, p.views, p.created_at,
        DATE_FORMAT(p.created_at, '%Y-%M-%d %H:%i') AS created_at_str
        FROM post p
        LEFT JOIN post_category c USING (category_id)
        LEFT JOIN member m USING (member_id)
        <include refid="searchWhere"/>
        ORDER BY p.post_id DESC
        LIMIT #{offset}, #{limit}
    </select>

    <!--  -->
    <select id="selectPostCount" parameterType="PostSearchCondition" resultType="int">
        SELECT COUNT(*)
        FROM post p
            LEFT JOIN post_category c USING (category_id)
        <include refid="searchWhere"/>
    </select>

    <!-- 게시글 상세 조회 -->
    <select id="selectPostDetail" parameterType="Long" resultType="PostDto">
        SELECT p.post_id, c.category, p.title, p.content, m.nickname, p.views, p.created_at,
            DATE_FORMAT(p.created_at, '%Y-%m-%d %H:%i') AS created_at_str
        FROM post p
            LEFT JOIN member m USING (member_id)
            LEFT JOIN post_category c USING (category_id)
        WHERE p.post_id = #{postId}
    </select>

</mapper>